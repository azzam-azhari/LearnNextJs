export default function layout({ children }) {
    return (
      <html lang="en">
        <head>
          <title>Next.Js</title>
        </head>
        <body>{children}</body>
      </html>
    );
  }
